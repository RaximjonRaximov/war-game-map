[project]
name = "war-game-map"
version = "0.2.0"
description = "Real-time multiplayer world domination game"
requires-python = ">=3.10"
dependencies = ["fastapi", "uvicorn[standard]"]

[tool.setuptools]
py-modules = ["main"]
